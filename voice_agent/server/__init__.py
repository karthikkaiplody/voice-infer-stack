"""The local web server behind the browser UI (`ui/`).

    live.py    serves the UI and streams events: replays fixtures, or runs the live agent
    tuning.py  the few settings the UI may change while the agent is stopped
"""
