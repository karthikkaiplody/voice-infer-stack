"""How a turn is measured, and what may be reported.

    contract.py        the metadata-only event contract: names, allowed fields, validation
    tracing.py         turns Pipecat's own spans into files and live events
    runtime_events.py  the bridge that produces contract events while the agent runs
"""
