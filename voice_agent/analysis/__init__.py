"""Read recorded turns. Standard library only, so these run without installing anything.

    measurement.py  the arithmetic: overlaps, sums, medians, end-of-speech
    budget.py       prints where the time went for a set of recorded turns
    viewer.py       renders one turn as a standalone HTML waterfall
"""
