# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Node leftovers from tooling
build/
ui/node_modules/
ui/dist/
ui/coverage/

# Local tool config, not part of the project
.mcp.json
.ignore
.claude/

# graft's local graph cache - regenerable, not committed (run `graft build`).
/graft/

# Conductor workspace scratch
.context/

# Traces you produce yourself. The committed ones are reference-traces.jsonl
# (one recorded turn) and scheduling-comparison.jsonl (what the tests read).
artifacts/live-traces.jsonl
artifacts/my-traces.jsonl

# Piper downloads voice models into the working directory
*.onnx
*.onnx.json
models/

# Agent context: local working notes, deliberately not part of the project
AGENTS.md
CLAUDE.md
